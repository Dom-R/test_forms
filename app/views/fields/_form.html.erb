<% @field_checkbox = Field.find(field.id) %>
<%= form_for @field_checkbox, :url => edit_sub_category_field_path(sub_category_id: @field_checkbox.sub_category_id, id: @field_checkbox), :method => "get" do |f| %>
  <%= f.text_field :val, :value => "", :required => true %>
  <%= f.submit "Add new Value" %>
<% end %>
